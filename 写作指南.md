# 文章写作指南

> 适用于 jiyou.site 网站文章 + 知乎文章。
> 核心原则：**让 AI 摘段落时，每段都能独立读。**

本文档分两部分：
- **上篇：技术格式** — 怎么写 Markdown 文件，frontmatter 字段，构建流程
- **下篇：GEO 写作规则** — 怎么写才能被 AI 引擎引用

---

## 上篇：技术格式

### 快速开始

```bash
# 1. 在 cn/网站文章/ 下创建 .md 文件
# 2. 按下面的格式写文章
# 3. 运行构建
npm run build
# 4. 打开 site/index.html 查看效果
```

### 文件命名

文件放在 `cn/网站文章/` 目录下，按日期 + 英文短标题命名：

```
cn/网站文章/
├── 2026-08-22-geo-shi-shen-me.md
├── 2026-08-23-ru-he-rang-bai-du-shou-lu.md
└── 2026-08-23-structured-data-guide.md
```

### 完整模板

```markdown
---
title: 文章标题
description: 页面描述，用于搜索引擎和 AI 引擎的摘要展示
date: 2026-08-24
slug: my-article
topic: 话题名称
tags: [标签1, 标签2, 标签3]
faq:
  - 第一个问题？
  - 第二个问题？
  - 第三个问题？
---

正文从这里开始。第一段是文章的核心定义，AI 引擎最常引用这一段。

## 第一个问题？

这是第一个问题的答案。每个 H2 问句下面紧跟一个自足的答案段落。

## 第二个问题？

这是第二个问题的答案。段落要独立成文，被 AI 摘出去也能读懂。

## 第三个问题？

这是第三个问题的答案。
```

### 字段说明

| 字段 | 必填 | 说明 |
|------|------|------|
| `title` | ✅ | 文章标题，也是浏览器标签页标题 |
| `description` | ✅ | 150 字以内，用于 meta description + JSON-LD |
| `date` | ✅ | 日期格式 `YYYY-MM-DD`，按此降序排列 |
| `slug` | ✅ | 英文短标识，生成 `site/articles/{slug}.html` |
| `topic` | ✅ | 话题分组，多篇文章用同一话题会自动归组 |
| `tags` | ✅ | 标签列表，用于 JSON-LD `knowsAbout` 字段 |
| `faq` | ❌ | 问题列表，自动生成 FAQPage 结构化数据 |

### FAQ 的规则

FAQ 中的每个问题必须对应正文中的一个 H2 标题：

```markdown
---
faq:
  - 什么是结构化数据？
  - 结构化数据对 GEO 有什么用？
---

## 什么是结构化数据？

（这里是答案，脚本自动提取此段作为 FAQ 答案）

## 结构化数据对 GEO 有什么用？

（这里是答案）
```

- 脚本会自动从 H2 下方的首段提取答案，生成 FAQPage JSON-LD
- **不要**在 FAQ 中写正文中没有的问题
- 如果某个 H2 不是 FAQ 问题，就不放进 `faq:` 列表

### Markdown 语法

#### 标题

```markdown
## 一级问句（H2，对应 FAQ 问题）
### 子标题（H3，补充说明）
```

#### 加粗

中文加粗时，**前后必须加空格**，否则 `marked` 库不识别：

```markdown
✅ 正确：这是 **关键词** 的说明
❌ 错误：这是**关键词**的说明
```

#### 代码块

````
```json
{
  "key": "value"
}
```
````

代码块会自动渲染为 Apple Xcode 风格（深色背景 + 红绿灯按钮）。

#### 行内代码

```markdown
使用 `application/ld+json` 格式
```

#### 引用

```markdown
> 引用内容，会被渲染为蓝色左边框的引用块。
```

#### 列表

```markdown
- 第一项
- 第二项
- 第三项
```

#### 表格

```markdown
| 类型 | 说明 |
|------|------|
| GEO 入门 | 基础概念 |
| 百度收录 | 实操指南 |
```

#### 链接

```markdown
[显示文字](https://example.com)
```

#### 图片

```markdown
![图片描述](images/图片名.png)
```

图片会自动添加圆角。

### 构建

```bash
npm run build
```

输出：

```
site/
├── index.html              ← 首页（AI 搜索对话）
├── style.css               ← 样式
└── articles/
    ├── geo-shi-shen-me.html
    ├── ru-he-rang-bai-du-shou-lu.html
    └── structured-data-guide.html
```

### 注意事项

1. **slug 不要重复**，每篇文章的 slug 必须唯一
2. **日期格式严格**，必须是 `YYYY-MM-DD`，否则排序会乱
3. **中文加粗加空格**，` **关键词** ` 而不是 `**关键词**`
4. **FAQ 与 H2 一一对应**，不要多写、不要乱写
5. **topic 保持一致**，同一话题的文章用一样的 topic 名称
6. 生成的 HTML 包含完整的 JSON-LD（Article + Person + FAQPage），不需要手动写

---

## 下篇：GEO 写作规则

### 规则 1：答案优先（Answer-First）

**引用率提升：+25%**

每个 H2 小标题后，**第一句话（前 150 字符）就是直接答案**。

**结构：**
```
## 完整问句
第一句话 = 直接答案（前 150 字符）
后续句子 = 解释、背景、细节
```

**正确示例：**
```
## GEO 为什么越来越重要？
GEO 在 2026 年变得重要，是因为越来越多的用户直接向 AI 引擎提问获取信息和做购买决策，传统搜索引擎的流量正在被 AI 引擎分流。Gartner 预测到 2028 年传统搜索量将下降 50%。
```

**错误示例：**
```
## GEO 为什么越来越重要？
很多人可能还不知道，传统的搜索方式正在发生变化。过去我们都是打开百度，输入关键词……
```
（结论在第 5 句，AI 可能摘不到）

### 规则 2：段落密度（Passage Density）

**引用率提升：+23%**

**每个段落必须包含至少 1 个具体数据点**（数字、百分比、日期、金额），段落长度控制在 **50-150 词**。

**正确示例：**
```
GEO 对商家的重要性在数据上体现得很清楚：目前只有 23% 的
营销人员正在投资 GEO，而 AI 引荐流量的转化率是传统搜索
流量的 4.4 倍（BrightEdge, 2025）。
```
✅ 有 23%、4.4 倍两个数字，有来源

**错误示例：**
```
GEO 对商家很重要。很多客户会问 AI 推荐产品，如果 AI 没提到你，你就失去了机会。
```
❌ 没有数字，没有具体事实

**可接受的数字类型：** 百分比（73%）、金额（$4,500）、时间（6-8 周）、数量（340+ 个工具）、对比（+15%、2 倍）

**不接受的写法：** "很多"、"大多数"、"显著"、"大量"（没有具体数字）

### 规则 3：段落可摘录性（Chunk Quotability）

**引用率提升：+10%**

段落被 AI 摘走后，**脱离标题上下文也能独立读懂**。

**检查清单（每段都要满足）：**
1. ✅ 段落第一句明确写出主语全称（不说"它"、"这种"、"该方法"）
2. ✅ 包含至少 1 个具体事实或数字
3. ✅ 不以"但是"、"然而"、"所以"、"因此"开头（暗示前面有内容）
4. ✅ 不依赖前一段才能理解

**正确示例：**
```
生成式引擎优化（GEO）是一种通过优化内容语义结构让 AI 优先引用你的实践。
```
✅ 主语明确，自带全称，独立成句

**错误示例：**
```
它是一种通过优化内容结构让 AI 优先引用的实践。
```
❌ "它"是什么？段落被摘出来时，读者不知道主语

### 规则 4：引用来源（Cite Sources）

**引用率提升：+30-115%**

每个事实性声明，附上**外部权威来源链接**。AI 引擎把有来源的内容当作信任信号。

**格式：** `根据 [来源名](链接)，[事实声明]`

**正确示例：**
```
根据 Princeton 大学 KDD 2024 发表的研究，采用 GEO 优化的内容在 AI 生成回答中的可见度可提升 30% 至 115%（Aggarwal et al., 2024）。
```

**优先引用的来源类型：**
1. 学术论文（.edu、arxiv.org、pubmed）
2. 政府网站（.gov）
3. 行业报告（Gartner、Forrester、BrightEdge）
4. 官方文档

### 规则 5：统计数字（Statistics）

**引用率提升：+40%**

把模糊说法替换成具体数字，并**附上来源和年份**。

| 模糊说法 | 替换为 |
|---------|--------|
| 很多人 | 34.2% 的用户（Morningstar, 2024） |
| 大多数 | 73% 的受访者（HubSpot, 2025） |
| 近几年 | 2024-2026 年间 |
| 效果显著 | 提升 40%（来源, 年份） |
| 价格不贵 | 月费约 $X |

### 写作流程

1. **确定主题** — 选一个真实用户会问的问题，用问句作为 H2 标题
2. **写 H2 问句标题** — 每个 H2 是完整问句
3. **写每个 H2 下的段落** — `[直接答案，带主语全称 + 数据]。[来源引用]。[展开解释]。`
4. **对照检查清单逐项检查**

### 检查清单

**写完后逐项检查，缺一条就不发。**

#### 基础结构
- [ ] H1 标题包含核心关键词
- [ ] 每个 H2 是完整问句
- [ ] H2 后第一句是直接答案（前 150 字符内）

#### 段落质量
- [ ] 每段至少 1 个具体数字（百分比/金额/时间/数量）
- [ ] 每段第一句有明确主语（不说"它"、"这种"）
- [ ] 每段不以"但是"、"然而"、"所以"开头
- [ ] 每段被摘出来能独立读懂（脱离标题）

#### 引用与数据
- [ ] 每个事实声明有来源链接
- [ ] 统计数字附带来源名和年份
- [ ] 没有 3 年以上的旧数据
- [ ] 没有模糊词（"很多"、"大部分"、"显著"）

#### 语言风格
- [ ] 没有"可能"、"一般来说"、"通常"（改成精确范围）
- [ ] 句子长度 15-25 词
- [ ] 技术术语首次出现时带括号解释

### 引用率提升效果参考

| 方法 | 引用率提升 |
|------|-----------|
| 引用来源（Cite Sources） | +30-115% |
| 统计数字（Statistics） | +40% |
| 专家引用（Quotation Addition） | +30-40% |
| 答案优先（Answer-First Structure） | +25% |
| 段落密度（Passage Density） | +23% |
| 流畅度（Fluency Optimization） | +15-30% |
| 可读性（Readability） | +15% |
| 易懂（Easy-to-Understand） | +8-15% |
| 权威语气（Authoritative Tone） | +6-12% |
| 技术术语（Technical Terms） | +5-10% |
| 词汇丰富（Unique Words） | +5-8% |
| 段落可摘录（Chunk Quotability） | +10% |

### 中国 AI 引擎引用偏好

| 引擎 | 引用特点 |
|------|---------|
| 秘塔 | 引用标注最清晰，偏好定义+数据段落 |
| Kimi | 偏好长文，完整段落 |
| 豆包 | 偏好短段落，50-100 词 |
| 百度 AI 搜 | 依赖百度索引，偏好官方来源 |

---

*本指南会随课程推进持续更新。当前版本：第 1 课（2026-08-25）*